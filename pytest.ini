[pytest]
testpaths = app/tests
# Coroutine tests are opted in explicitly with @pytest.mark.asyncio (strict is
# pytest-asyncio's default). Without the plugin installed those tests do not
# merely skip — they fail with "async def functions are not natively
# supported", so pytest-asyncio is a required test dependency.
asyncio_mode = strict
